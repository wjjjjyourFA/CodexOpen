manual_loop_session_exporter:
  odom_topic: /state_estimation
  cloud_topic: /registered_scan
  finalize_service: finalize
  sync_queue_size: 100
  sync_slop: 0.05
  conversion:
    input_cloud_is_global: true
  io:
    output_directory: /tmp/codexopen_manual_loop_session
